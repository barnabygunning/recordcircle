/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['via.placeholder.com', 'i.scdn.co', 'images.unsplash.com'],
  },
}

module.exports = nextConfig
